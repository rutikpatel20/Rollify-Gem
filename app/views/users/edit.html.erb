<%= form_with(model: @user) do |form| %>
  <%= form.collection_check_boxes :role_ids, Role.all, :id, :name %>
  <%= @user.errors[:roles] %>
  <br>
  <%= form.submit class:"btn-primary border-0 rounded-pill shadow p-3" %>
<% end %>
